apiVersion: v1
kind: Service
metadata:
  name: money-transfer-service
  labels:
    app: money-transfer-service
spec:
  type: ClusterIP
  ports:
    - port: 80
      targetPort: 8080
      protocol: TCP
      name: http
  selector:
    app: money-transfer-service
